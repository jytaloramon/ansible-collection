[healthcheck]
listen = {{ php_dir.run }}/healthcheck.sock
listen.owner = php
listen.group = php
listen.mode = 0660

user = php
group = php

pm = static
pm.max_children = 1

pm.status_path = /status

ping.path = /health
ping.response = "{\"status\":\"OK\"}"

